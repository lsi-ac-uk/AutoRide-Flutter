import 'package:flutter/material.dart';

// This clas contain all colors we want to use in our project.
class ColorManager{
  ColorManager._();

  static const mainBackGroundColor = Color.fromARGB(255, 255, 238, 204);
  static const buttonColor = Color.fromARGB(255, 253, 197, 87);
  static const appBarColor = Color.fromARGB(255, 255, 255, 255);
  static const successColor = Colors.green;
  static const failedColor = Colors.red;
  static const textColor = Colors.black;
  static const hintTextColor = Colors.grey;


}